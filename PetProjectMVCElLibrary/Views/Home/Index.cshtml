@model TextField
@{
    // string strTitle = @Model.Title;
    string strTitle = "тайтл";
    ViewBag.Title = strTitle;
}
<div>
    <a asp-area="" asp-controller="Book" asp-action="Index">Главная</a>
    @* @Html.Raw(Model.Text) *@
    @Html.Raw("Текст")
</div>